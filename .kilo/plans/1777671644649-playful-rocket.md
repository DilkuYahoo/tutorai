# Plan: Add Quality Flag Display to Stat Cards

## Objective
Display quality flags (Act/Exp/Fcst) on the stat cards in the Energy-Mate dashboard so users can understand the type of data they're viewing (Actual, Expected, or Forecast).

## Files to Modify

### 1. Frontend: StatCard Component
**File**: `app/energy-mate/frontend/src/components/StatCard.jsx`
**Changes**: 
- Accept `quality` prop (optional)
- Add quality badge at top-right corner of card (small, subtle)
- Display colored badge using existing CLAUDE.md color scheme
- Badge shows abbreviated label (Act/Exp/Fcst) with full label on hover

### 2. Frontend: Dashboard Component  
**File**: `app/energy-mate/frontend/src/pages/Dashboard.jsx`
**Changes**:
- Pass `quality` from live data to both StatCard components (import rate and FiT rate cards)
- Move `qualityBadge` function to shared or extract into StatCard, or keep in Dashboard and use a simpler badge variant

### 3. Backend: get_live Lambda
**File**: `app/energy-mate/backend/lambda/api/get_live.py`
**Status**: Already includes `quality` in response (line 84) - no changes needed

### 4. Frontend: Mock Data
**File**: `app/energy-mate/frontend/src/mock.js`
**Changes**: Add `quality` field to mock live data for testing

## Implementation Details

### StatCard.jsx Changes
- Add `quality` prop parameter
- Render small pill badge in top-right corner when quality is provided
- Color mapping: Act=green, Exp=blue, Fcst=amber (matching CLAUDE.md)
- Minimal size: `text-[10px]` px-1 py-0.5, absolutely positioned
- Uses same color classes as Dashboard's qualityBadge

### Color Scheme (from CLAUDE.md)
- `Act` (Actual): `bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300`
- `Exp` (Expected): `bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300`  
- `Fcst` (Forecast): `bg-amber-100 text-amber-700 dark:bg-amber-900 dark:text-amber-300`

### Dashboard.jsx Changes
- Pass `quality={live.quality}` to both StatCard components
- Import rate and FiT rate cards both show same quality (same data source)

## Validation
- Quality flag already returned by get_live Lambda at line 84
- GET /dashboard/live response includes quality field
- Mock data needs quality field added
- No backend changes required
- Minimal frontend changes

## Notes
- Quality is per-interval, so both rate cards show the same badge
- Helps users distinguish actual readings vs. forecast data
- Consistent with existing UI color scheme