# Deployment Configuration for auth-login Lambda
# Update these values before deploying

# Lambda Function Configuration
FUNCTION_NAME=auth-login-user-info-lambda
RUNTIME=python3.13
HANDLER=user_info.lambda_handler
TIMEOUT=30
MEMORY_SIZE=128

# Environment Variables
COGNITO_USER_POOL_ID=ap-southeast-2_GWzLKI17V
REGION=ap-southeast-2

# IAM Role (create or use existing)
# ROLE_ARN=arn:aws:iam::ACCOUNT_ID:role/lambda-execution-role

# Deployment Settings
S3_BUCKET=your-deployment-bucket
S3_KEY=lambda/auth-login/user_info.zip
